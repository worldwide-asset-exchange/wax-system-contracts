
## 2026-09-26 — PRs open, deploy runbook ready
- **#136** WBP-2022 fix (SYS-017), two review passes, 214/214, ratchet 34/0. **#135** CHANGELOG dup. **WBP-2025** deploy tooling PR
  (number in the ticket). Findings 25 = 1H/7M/12L/5I, 18 fixed, 7 open (SYS-017 open until #136 ships in a tag after 3.3.1).
- **Deploy runbook for wax-3.3.1:** `chain/.wt-wsc-2025/deploy/out/wax-3.3.1/RUNBOOK.md` (+ the .trx.json / packed hex / params
  json / requested.json of that same run — regenerate = new set). Plan: raise max_inline_action_size 300000→800000, set code+abi
  (packed 416,745 B), restore; proposals `inccijfae → setcijfae → rescijfae`, proposer admin2.wax, approvers admin1/2/3.wax@active.
  **Blocker for Aaron: admin2.wax NET available 256 KB < 417 KB** — stake/powerup NET first (or another proposer with NET + ~420 KB RAM).
  Pre-deploy: cancel any proposal naming `removerefund` (admin2.wax open: thqtoz, vdyfde — check them). Post-deploy: `verify`, then the
  docs PR (README provenance row + SECURITY.md table), Review→Done tickets, PDF regen.
